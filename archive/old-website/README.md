# Old Website Archive

This folder contains the downloaded copy of the previous static website and the crawler used to mirror it.

- `source/` is the original site snapshot.
- `crawl-source.mjs` is the one-off script used to download the old content.

The active React/Vite website is in `src/`, with deploy assets generated into the repository root by `npm run build:pages-root`.
